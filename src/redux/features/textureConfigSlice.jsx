import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

export const textureConfigSlice = createSlice({
    name: 'textureConfig',
    initialState,
    reducers: {
        
    }
})

export default textureConfigSlice.reducer;