import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

export const glbConfigSlice = createSlice({
    name: 'glbConfig',
    initialState,
    reducers: {
        
    }
})

export default glbConfigSlice.reducer;